import { Box } from './Box'

## Guidelines

These guidelines are an example of how importing MDX works. Components like `Box` are supported as well for things like <Box color="tomato">overriding inline text</Box>.

