import { Text } from './Text'

## Guidelines

These guidelines are an example of how importing MDX works. Components like `Text` are supported as well for things like <Text color="tomato">overriding inline text</Text>.

