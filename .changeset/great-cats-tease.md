---
'renoun': minor
---

Renames `EntryGroup` to `Collection` to better describe a set of directories. Since renoun is lower-level, this makes it easier to describe in documentation e.g. create a `collections.ts` file that encapsulates all directory definitions.
