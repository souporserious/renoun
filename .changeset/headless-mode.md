---
'renoun': minor
---

Introduces the ability for the `renoun` CLI to run renoun-aware applications and templates directly from
`node_modules` with file shadowing, plus a new `renoun eject` command.
