---
'renoun': patch
---

Improves `Tokens` component by applying necessary theme styles through the `RootProvider` component. This avoids the immediate container having to specify anything and decouples the responsibility.
