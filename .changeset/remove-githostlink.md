---
'renoun': major
---

Removes `GitProviderLink` in favor of `<Link variant="repository">`. This also removes `GitProviderLogo` in favor of a new `Logo` component. Now `<Logo variant="gitHost">` can be used for rendering git host logos.
