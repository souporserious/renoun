---
'renoun': minor
---

Adds `commit` and `releaseTag` variants to the `Link` component along with `getCommitUrl` and `getReleaseTagUrl` methods on the `Repository` class for constructing URLs to specific commits and release tags.
