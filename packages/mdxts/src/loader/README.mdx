# mdxts/loader

When a TypeScript file pattern is provided, the closest `README.mdx` or MDX file with the same name will be used (e.g. `Button.tsx` and `Button.mdx`). See [routing](/docs/routing) for more information.

<Note>
  The loader is automatically configured when using the [Next.js
  plugin](/packages/next).
</Note>
