# omnidoc

The primary package for Omnidoc that provides a utility for working with a collection of module imports.

```ts filename="data.ts"
import { createSource } from 'omnidoc'

export const allDocs = createSource('docs/**/*.mdx', {
  baseDirectory: 'docs',
})
```
