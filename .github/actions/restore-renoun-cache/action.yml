name: Restore renoun cache
description: Resolve and restore the renoun filesystem cache for CI jobs
inputs:
  node-version:
    description: Node major version used by the workflow cache key segment
    required: false
    default: '22'
runs:
  using: composite
  steps:
    - name: Resolve renoun cache token
      id: renoun-cache-token
      shell: bash
      run: echo "token=$(node packages/renoun/src/cli/index.ts cache-token)" >> "$GITHUB_OUTPUT"

    - name: Restore renoun cache
      uses: actions/cache@cdf6c1fa76f9f475f3d7449005a359c84ca0f306
      with:
        path: |
          .renoun/cache/fs-cache.sqlite
          .renoun/cache/fs-cache.sqlite-shm
          .renoun/cache/fs-cache.sqlite-wal
        key: ${{ runner.os }}-node${{ inputs.node-version }}-renoun-cache-${{ github.workflow }}-${{ steps.renoun-cache-token.outputs.token }}-${{ hashFiles('pnpm-lock.yaml') }}
        restore-keys: |
          ${{ runner.os }}-node${{ inputs.node-version }}-renoun-cache-${{ github.workflow }}-${{ steps.renoun-cache-token.outputs.token }}-
