{
  "name": "omnidoc",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "build": "turbo run build",
    "build:site": "pnpm build --filter=@apps/site",
    "build:examples": "pnpm build --filter=@examples/*",
    "dev": "turbo run dev",
    "dev:site": "pnpm dev --filter=omnidoc --filter=@apps/site",
    "dev:examples": "pnpm dev --filter=omnidoc --filter=@examples/*",
    "test": "turbo run test",
    "format": "prettier --write '**/**/*.{js,ts,tsx,json,css}'",
    "ci:version": "pnpm changeset version && pnpm install --no-frozen-lockfile && git add .",
    "ci:publish": "pnpm build && pnpm publish -r --no-git-checks && pnpm changeset tag",
    "clean": "node -e \"(function rm(directory) { ['.next', '.turbo', 'node_modules', 'dist'].includes(path.basename(directory)) ? fs.rmSync(directory, { recursive: true, force: true }) : fs.existsSync(directory) && fs.statSync(directory).isDirectory() && fs.readdirSync(directory).forEach(filePath => rm(path.join(directory, filePath))); })('.');\""
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.7",
    "@swc/core": "^1.7.21",
    "@swc/jest": "^0.2.36",
    "@types/jest": "^29.5.12",
    "@types/node": "^22.5.1",
    "concurrently": "^8.2.2",
    "jest": "^29.7.0",
    "next": "14.2.7",
    "prettier": "3.3.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tsup": "8.2.4",
    "turbo": "^2.1.0",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": "8.15.8"
  },
  "packageManager": "pnpm@8.15.8"
}
