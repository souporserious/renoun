export const metadata = {
  title: 'Installation',
  summary: 'How to install and set up the docs example.',
  order: 2,
}

To get started, clone the repository and install dependencies:

```bash
npm install
```

Then run the development server:

```bash
npm run dev
```

You should now be able to view the docs site locally.
