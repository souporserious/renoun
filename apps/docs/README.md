# @renoun/docs

A documentation starter that turns MDX content into a polished, full-featured site.

[Demo](https://docs.renoun.dev)

## Getting Started

Use the [`create-renoun`](https://www.npmjs.com/package/create-renoun) CLI to bootstrap this template:

```bash
npx create-renoun --template docs
```
