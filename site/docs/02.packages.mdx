export const metadata = {
  title: 'Packages',
}

# Package Exports

The `mdxts` package exports a main package and a set of sub-packages.

<PackageExports
  name="mdxts"
  context={require.context('../../mdxts/src', true, /README\.mdx$/, 'lazy')}
/>
