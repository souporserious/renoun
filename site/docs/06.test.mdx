```tsx live
import { useState } from 'react'
import { Code, Navigation } from 'mdxts/components'

/**
 * Say hello.
 *
 * @example
 * <Hello name="Penny" />
 */
function Hello({ name }: { name: string }) {
  return <div>Hello, {name}</div>
}

const foo = { hello: 'world' }
```
