# Props

List props for the closest file if used in a README file.

```mdx
import { Props } from 'mdxts'

<Props />
```

For rendering specific props, you can pass a component as a prop.

```mdx
import { Props } from 'mdxts'
import { Button } from './Button'

<Props of={Button} />
```
