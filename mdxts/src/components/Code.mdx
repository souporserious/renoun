## Code Blocks

```tsx filename="useCounter.ts"
import { useState } from 'react'

export function useCounter(initialValue: number = 0) {
  const [count, setCount] = useState(initialValue)
  return {
    count,
    increment: () => setCount(count + 1),
    decrement: () => setCount(count - 1),
  }
}
```

```tsx filename="Counter.tsx" lineNumbers highlight="5-7,9"
'use client'
import { useCounter } from './useCounter'

export default function Counter({
  initialCount = 0,
}: {
  initialCount: number
}) {
  const { count, decrement, increment } = useCounter(initialCount)
  return (
    <div>
      <button onClick={decrement}>-</button>
      <span>{count}</span>
      <button onClick={increment}>+</button>
    </div>
  )
}
```

```tsx filename="preview.tsx"
<Counter initialCount={2} />
```

<Preview source="preview.tsx" />

## Components

import Counter from './counter/Counter.tsx'

<Code source="./counter/Counter.tsx" />

<Code source="./counter/useCounter.ts" />

<Playground>
  <Counter initialCount={3} />
</Playground>
