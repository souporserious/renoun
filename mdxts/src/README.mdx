# mdxts

This is the primary package for the `mdxts` library that provides a utility for working with a collection of module imports.

```ts filename="data.ts"
import { createSource } from 'mdxts'

export const allDocs = createSource('docs/**/*.mdx', {
  baseDirectory: 'docs',
})
```
